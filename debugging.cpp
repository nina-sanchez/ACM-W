#include <iostream>

using namespace std;

int main() {
    int user-input;
    int sum = 0

    // using a for loop to get numbers from the user 5 times
    for (i = 0; i < 5; ++i) {
        cout << "Enter a number: ";
        cin >> userInput;

        // add the entered number to the sum
        sum += number:
    }

    cout << "Sum of the entered numbers: " << sum << endl;

    return
}

